services:
  pill_posting:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - .:/app

  mongo:
    image: mongo:latest
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db

volumes:
  mongo_data:
